/*
 * OmniRoute native backend — identity constants.
 *
 * Single source of truth for the executable name and version reported by
 * `--help` / `--version` and the default startup line.
 */
#ifndef OMNIROUTE_VERSION_H
#define OMNIROUTE_VERSION_H

#define OMNIROUTE_NATIVE_NAME "omniroute-native"
#define OMNIROUTE_NATIVE_VERSION "0.1.0"

#endif /* OMNIROUTE_VERSION_H */
